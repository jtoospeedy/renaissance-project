# Drop your trailer here

Place `trailer.mp4` (and optionally `trailer.webm`) in this directory.

The Home page (`app/page.jsx` via `components/TrailerVideo.jsx`) picks it up automatically from `/trailer.mp4`.

For real photos, create an `img/` subfolder and reference files as `/img/your-file.jpg` from the data files in `/lib`.
